services:
  okx-options-bot:
    container_name: okx-options-bot
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - okx_options_bot_data:/app/data

volumes:
  okx_options_bot_data:
