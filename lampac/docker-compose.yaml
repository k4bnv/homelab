services:
  lampac:
    container_name: lampac
    image: ghcr.io/lampac-nextgen/lampac
    restart: unless-stopped
    ports:
      - "9118:9118"
    networks:
      - frontend

networks:
  frontend:
    external: true
